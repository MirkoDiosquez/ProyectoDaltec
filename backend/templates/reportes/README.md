Place the report template file in this directory with the exact name:

TC-SGC-03_NC-OM_rev.06C.xlsm

The report generator will use this workbook to preserve macros and format.
If the template file does not exist, the system will generate a fallback .xlsx report.
